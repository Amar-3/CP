class Solution {
public:
    bool isPowerOfTwo(int n) {
     return n>0 && (n & (n-1) )==0;
    }
};

// logic is numser and its n-1 attempted to & operation at bit level willl result 000000000..... 
//else their will always be atleast 1  1 left after computaton
